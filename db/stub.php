<?php require 'db_interface.php';
/**
 * Stub to test database functions
 */
    // println("Hello, World!");
    // add_employee("John", "Doe", null, "2021-12-15", "johndoe@email.com", 1234567890, "123 Some Street", date("Y-m-d"), "General manager", 1, "johndoe", md5("johndoe"));
    // add_employee("Joe", "Dohn", null, "2021-12-15", "joedohn@email.com", 1234567890, "123 Some Street", date("Y-m-d"), "Cashier", 1, "joedohn", md5("joedohn"));
    // add_customer("Some", "User", null, null, "someuser@email.com", null, null, date("Y-m-d"), null, null, "someuser", md5("someuser"));
    // add_customer_with_email("New", "User", null, "newuser@email.com", "newuser", md5("newuser"));
    // __insert_into_table("Time_", [10]);

    // remove_user("johndoe");
    // remove_manager(0);

    // add_restaurant_booking("newuser@email.com", date("Y-m-d"), "13:01", 10, null);

    // println(find_restaurant_booking_with_email("newuser@email.com")["BookedDate"]);
    // remove_restaurant_booking_with_booking_id(3);
    // println(remove_restaurant_booking_with_email("newuser@email.com"));

    // $username = "admin"; $user = null;
    // switch (find_user($username)["AdminPrivilege"]) {
    //     case 0: // Customer
    //         $user = find_customer_with_username($username);
    //         break;
    //     case 1: // Employee
    //         $user = find_employee_with_username($user);
    //         break;
    //     case 2: // Manager
    //         $user = find_manager_with_username($username);
    //         break;
    // }

    // $user["FName"]; // User's first name

    // add_restaurant_booking("newuser@email.com", date("Y-m-d"), "14:00", 10, null);
    // add_restaurant_booking("newuser@email.com", date("Y-m-d"), "13:00", 10, null);
    // add_restaurant_booking("newuser@email.com", date("Y-m-d"), "16:15", 10, null);
    // add_restaurant_booking("newuser@email.com", date("Y-m-d"), "01:05", 10, null);
    // add_restaurant_booking("newuser@email.com", date("Y-m-d"), "16:35", 10, null);
    // println(avail_seats_for_time("15:00"));
    
    // update_customer("_huybui", "Huy", null, null, null, null, null, null, null);
    // update_fname("_huybui", "Huy");


    // find_employees_managed_by(1)

    println(get_admin_priv("admin"));
?>